function suma() {
  let numero_uno = parseInt(prompt("Dime un numero que quieres sumar"));
  let numero_dos = parseInt(prompt("Dime el segundo numero que quieres sumar"));
  window.alert(numero_dos + numero_uno);
}
