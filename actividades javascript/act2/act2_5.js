function mas_grande(){
    let numero_uno = prompt('dime un numero');
    let numero_dos = prompt('dime otro numero');
    if (numero_uno > numero_dos) {
        alert(numero_uno)
    } else {
        alert(numero_dos)
    }
}